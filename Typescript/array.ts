// Create an array of vaccine names
let vaccines: string[] = ["Covaxin", "Covishield"];

// Add another vaccine to the array
vaccines.push("Sputnik V");

// Print the vaccine names using a for loop
for (const vaccine of vaccines) {
    console.log(vaccine);
}
